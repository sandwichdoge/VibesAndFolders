name: Build VibesAndFolders

on:
  push:
    branches: [ "main", "master" ]
  pull_request:
    branches: [ "main", "master" ]
  release:
    types: [created]

permissions:
  contents: write

jobs:
  build:
    name: Build ${{ matrix.os }}
    runs-on: ${{ matrix.runner }}
    
    strategy:
      matrix:
        include:
          - os: linux
            runner: ubuntu-latest
            artifact: VibesAndFolders-linux-amd64.tar.gz
          - os: windows
            runner: windows-latest
            artifact: VibesAndFolders-windows-amd64.zip
          - os: macos
            runner: macos-latest
            artifact: VibesAndFolders-macOS.zip

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: 'stable'

      # Install platform-specific dependencies
      - name: Install Linux dependencies
        if: matrix.os == 'linux'
        run: |
          sudo apt-get update
          sudo apt-get install -y gcc libgl1-mesa-dev xorg-dev

      - name: Install macOS dependencies
        if: matrix.os == 'macos'
        run: |
          # macOS already has the necessary tools
          echo "macOS build environment ready"

      # Build the application natively
      - name: Build Application
        run: go build -o VibesAndFolders${{ matrix.os == 'windows' && '.exe' || '' }} ./cmd/vibesandfolders

      # Package the builds
      - name: Package Linux Build
        if: matrix.os == 'linux'
        run: tar czvf ${{ matrix.artifact }} VibesAndFolders

      - name: Package Windows Build
        if: matrix.os == 'windows'
        shell: pwsh
        run: Compress-Archive -Path VibesAndFolders.exe -DestinationPath ${{ matrix.artifact }}

      - name: Package macOS Build
        if: matrix.os == 'macos'
        run: zip ${{ matrix.artifact }} VibesAndFolders

      # Upload artifacts for non-release builds
      - name: Upload Artifacts (CI)
        if: github.event_name != 'release'
        uses: actions/upload-artifact@v4
        with:
          name: VibesAndFolders-${{ matrix.os }}
          path: ${{ matrix.artifact }}

      # Upload to release if this is a release event
      - name: Upload Release Assets
        if: github.event_name == 'release'
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: gh release upload ${{ github.event.release.tag_name }} ${{ matrix.artifact }} --clobber
